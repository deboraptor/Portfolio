/* TODO: revoir, ça marche po trop */
        var largeur = frame.innerWidth;
        var hauteur = frame.innerHeight;
        document.getElementById("cadre").style.width = (largeur) + "px";
        document.getElementById("cadre").style.height = (hauteur) + "px";